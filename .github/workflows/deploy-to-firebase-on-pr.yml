# =============================================================================
# このワークフローは無効化されています
# =============================================================================
# upstream (hellomuthu23/planning-poker) から引き継いだファイルですが、
# 社内利用版ではプルリクエストでのプレビューデプロイは行いません。
#
# デプロイは deploy-to-firebase.yml (v* タグプッシュ時) のみで実行されます。
#
# 将来的にプレビュー機能が必要になったら、このファイルを書き換えて有効化する
# ことができます。
# =============================================================================

name: (Disabled) Deploy preview on PR

on:
  # 有効にするトリガーがないためワークフローは実行されない
  workflow_dispatch:
